# Optional: only needed if you've deployed the backend in /server (see
# server/README.md). This tells the frontend's Document Converter tool
# where to send files for true Word/PowerPoint/Excel <-> PDF conversion.
# Leave this unset if you're not running the backend — every other tool
# on the site works fine without it.
# VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=https://filelab-server.onrender.com

